import React from "react";
import styled from "styled-components";

function SideIcons() {
  return <SideIconsWrapper></SideIconsWrapper>;
}

export default SideIcons;
const SideIconsWrapper = styled.div`
  background-color: wheat;
`;
